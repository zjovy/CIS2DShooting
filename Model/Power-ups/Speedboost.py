import pygame
import game

# increase in speed
def speed_boost ():
  if(move_right):
        x += 4
    if(move_left):
        x -= 4
    if(move_up):
        y -= 4
    if(move_down):
        y += 4
